[servers]

%{~ for i in web }
${i["name"]}   ansible_host=${i["network_interface"][0]["nat_ip_address"]}
%{ endfor ~}

%{~ for i in vm-each }
${i["name"]}   ansible_host=${i["network_interface"][0]["nat_ip_address"]}
%{ endfor ~}

%{~ for i in vm-disk }
${i["name"]}   ansible_host=${i["network_interface"][0]["nat_ip_address"]}
%{ endfor ~}
